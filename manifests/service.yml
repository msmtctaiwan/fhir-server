apiVersion: v1
kind: Service
metadata:
    name: "aksaks5-0b6e"
    labels:
        app: "aksaks5-0b6e"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "aksaks5-0b6e"